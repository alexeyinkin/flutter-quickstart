import 'package:flutter/widgets.dart';

class QuickSpacing extends StatelessWidget {
  const QuickSpacing();

  @override
  Widget build(BuildContext context) {
    return const SizedBox(width: 10, height: 10);
  }
}
